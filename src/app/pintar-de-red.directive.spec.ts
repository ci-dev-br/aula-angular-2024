import { PintarDeRedDirective } from './pintar-de-red.directive';

describe('PintarDeRedDirective', () => {
  it('should create an instance', () => {
    const directive = new PintarDeRedDirective();
    expect(directive).toBeTruthy();
  });
});
