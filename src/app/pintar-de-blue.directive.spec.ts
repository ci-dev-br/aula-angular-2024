import { PintarDeBlueDirective } from './pintar-de-blue.directive';

describe('PintarDeBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new PintarDeBlueDirective();
    expect(directive).toBeTruthy();
  });
});
